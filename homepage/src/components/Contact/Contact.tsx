import { 
    Box, Container, Typography,
} from '@mui/material';

const Contact = () => {
    return (
        <Container component="main">
            To be created
        </Container>
    );
};

export default Contact